<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        新規レシピ作成
      </h1>
    </div>
  </div>
</section>

<div style="margin-top: 50px;">
    <%= form_for @recipe do |f| %>

        <%= f.label :名前%>
        <%= f.text_field :title%>

        <%= f.label :作り方%>
        <%= f.text_area :body%>

        <%= f.label :写真%>
        <%= f.attachment_field :image%>

        <%= f.submit "投稿"%>

    <% end %>
</div>

